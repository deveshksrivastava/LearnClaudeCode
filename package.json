{
  "name": "shopfast",
  "version": "1.0.0",
  "description": "ShopFast — Full-stack e-commerce app. FastAPI backend (port 8000) + React/Vite frontend (port 5173). Run 'npm run dev' from this folder to start both servers together.",
  "private": true,
  "scripts": {
    "dev": "concurrently --names \"api,web\" --prefix-colors \"cyan,magenta\" \"uvicorn app.main:app --reload\" \"npm run dev --prefix frontend\"",
    "dev:api": "uvicorn app.main:app --reload",
    "dev:web": "npm run dev --prefix frontend"
  },
  "engines": {
    "node": ">=18",
    "python": ">=3.9"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
