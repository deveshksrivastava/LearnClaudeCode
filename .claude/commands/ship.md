Run the full test suite with pytest.
If any tests fail, fix them in main.py first.
Once all tests pass, stage all changed files.
Write a descriptive git commit message summarising
what changed. Then don't commit. Wait for input command to push the code.
